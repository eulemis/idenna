Place your WOFF2 font files here. Recommended: generate a variable or subset WOFF2 for "Inter" (weights used by your app) and name them `Inter-Regular.woff2`, `Inter-Variable.woff2`, etc.

This folder is intentionally included so the service worker can precache the fonts.
